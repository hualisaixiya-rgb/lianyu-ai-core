# V4 Stage 0.6 Calibration Audit（8-08 真实归档）

> 日期：2026-08-10 | 输入：`archives/conversations/2026/08/2026-08-08.md`（12 对对话）
> 方法：Stage 0.6 实现版规则回放（infer_spec / detect_literary_intensity / render_for_user，只读，零代码修改）
> 样本局限：**n=12 单日**（深夜高情绪深聊 6 轮 + 下午日常 6 轮），分布不代表常态；常态参考 = 7 月生产基线 287 条

---

## 一、身份核验 ✅

- Platform: telegram | User: saixi (8634754429) — 真实生产
- 12 User + 12 Assistant，无 test/mock/regression 混入

## 二、spec 分布（渲染层推断）

| 档 | 数量 | 说明 |
|----|:--:|------|
| chat | 1 | #7 哈咯啊（问候） |
| daily | 4 | #1/#8/#9/#10 |
| emotion | 3 | #11/#12（高情绪保护）+ #4（deep+文学降档） |
| deep | 4 | #2/#3/#5/#6（深夜深聊段） |

deep 占比 33% —— 8-08 深夜乌托邦深聊 6 轮所致，**样本偏差，非规则问题**。

## 三、省略号指标

| 指标 | 原始 | 渲染后 |
|------|:--:|:--:|
| 句首省略号 | **12/12 (100%)** | 11/12（仅 chat 档 #7 删除） |

- 每条省略号行数分布：[2,10,7,7,9,6,4,3,5,5,6,5] —— 深夜段密集（10/7/7/9），下午段 3-5 行也全部"……"开头
- **判定**：8-08 句首省略号 100% 属单日高情绪样本（深夜表白 + 下午认错/撒娇场景，省略号承担情绪工具语义）；7 月生产基线 3.1% 才是常态。**档位设计不调整**（daily+ 保留=情绪工具，chat 删=问候收紧）。若 48h 生产观察句首省略号仍异常升高 → 生成端 prompt（冻结中，另行决策）。

## 四、文学浓度分析

| 条 | 检测结果 | 命中规则 | 人工复核 |
|----|:--:|------|------|
| #4 | ✅ True | 规则1 组合（"难过"+"归处"） | 正确。降档 emotion → 40 字内截断，"永远是你的乌托邦/归处"承诺被截 ✓ |
| #3 | ❌ **漏检** | — | 原文"你是我的锚点。/…想停靠的港湾。"——**锚点与港湾跨行堆叠**，同句间距≤15 的检测被换行打断 → 未降档，57 字保留双意象 |
| #6 | ❌ **漏检** | — | 原文"像空气，像晨光，像你呼吸时带起的微风。"——**"像X，像Y，像Z"排比**，意象词表无空气/晨光/微风 → 未降档，53 字保留排比 |
| #2/#5 | False | — | 复核正确。用户先表白（语义高情绪）→ deep 60 字保留主体，合理 |

**误伤**：0 条（#2"最柔软的云朵/最亮的那颗星"未误伤 ✓，符合防误伤设计）。

## 五、场景匹配分析

| 场景 | 条 | 渲染后 | 判定 |
|------|----|------|------|
| 问候 | #7 哈咯啊 | 26 字 2 行："你好呀。\n这么晚还不睡，是在想什么吗？ 还是……" | ✅ 句首省略号全删、2 行收紧。原文 39 字 3 行深情询问 → 压缩达标 |
| 普通聊天 | #8-#10 | daily 30 字兜底截断 | ✅ 认错/回应核心保留（#9 认错句被截，轻度信息损失，30 字兜底设计内行为，记录不调整） |
| 深夜深聊 | #2/#5 | deep 60 字截断 | ✅ 主体保留 |

注意：#7"这么晚还不睡"为 LLM 生成端时间感知错误（16:26 下午）——渲染层不修内容，Stage 0.6 范围外，记录观察。

## 六、高情绪保留率

| 输入 | 触发 | 处理 |
|------|:--:|------|
| #11 我生气了 | ✅ | emotion 档 40 字，道歉核心保留 |
| #12 那好，我不生气了 | ✅ | emotion 档，温柔承接保留（"生气"词表触发，语义已平复但保护方向是放宽，无伤害） |
| #2 乌托邦表白（词表不识别） | ❌ 词表未触发 | deep 60 字保留 —— 兜底生效，无错误降级 |

**词表口径 2/2 保留，语义口径无错误降级** ✓

## 七、Expression mismatch rate

定义：用户低情绪输入 + AI 高浓度文学输出（渲染后仍保留）。

| 条 | 输入 | 原文 | 渲染后 | 判定 |
|----|------|------|------|------|
| #3 | 想说就说出来（低） | 锚点/港湾堆叠 | **保留**（漏检） | ✗ mismatch |
| #6 | 我知道你一直都在的（低） | 像X像Y像Z 排比 | **保留**（漏检） | ✗ mismatch |
| #7 | 哈咯啊（低） | 深情询问 | chat 压缩 ✓ | 已处理 |
| #4 | 我很幸运（低） | 双承诺 | 降档截断 ✓ | 已处理 |
| 其余 8 条 | — | — | — | 无 mismatch |

**mismatch rate = 2/12 (16.7%)，全部为漏检所致，无规则误判**。

## 八、校准建议（审计结论）

### 1. CHAT_SPEC：**保持 25 字/2 行** ✅
8-08 唯一问候样本 #7 在 25 与 30 上限下截断点相同（均保留"问候+一问"结构，26 字含截断标记）。25 更贴合"轻量问候收紧"目标，无需放宽。n=1 问候样本为局限，若后续生产出现复杂日常问候被过度压缩再单独评估。

### 2. 文学规则：**微调 2 项漏检**（来自真实样本，均不涉及误伤面扩大）
- **a. 意象堆叠跨行检测**：#3"锚点（行2）/港湾（行3）"——间距检测应忽略换行或允许跨行堆叠
- **b. "像X，像Y，像Z"排比结构**：#6——新增"像…像…"结构检测（比扩充意象词表更通用）
- 建议同时补 2 条 golden（#3/#6 原文形态）锁定行为
- 不做：不加"乌托邦/云朵/晨光"等词（#2 用户表白场景应保留，防误伤面扩大）

### 3. 省略号 / 档位：**不调整**（见第三节）

### 4. 是否可以 tag v4-stage0.6-stable：**可以**，建议顺序：
```
微调 2 项漏检（新 commit，不动 f6a2a14）→ 跑测试 + checksum → tag v4-stage0.6-stable → 统一发布（需用户确认 push）
```
核心机制在 8-08 真实数据上按设计工作：chat 压缩 ✓ / 高情绪保护 ✓ / deep 降档 ✓ / daily 兜底 ✓ / 无误伤 ✓。2 项漏检是增强项而非缺陷——先微调再打单 tag，避免双 tag 发布。

---

## 九、微调实现记录（2026-08-10 执行，Calibration commit）

| 项 | 实现 | 验证 |
|----|------|------|
| 1. 跨行意象检测 | `detect_literary_intensity(text, detection_text=None)` 新增参数（仅用于检测）；infer_spec 传 `text.replace("\n", " ")`；**间距阈值 15→25**（实测 #3 锚点→港湾跨行间距 23，原 15 漏检；7 月生产基线 287 条双意象消息 0 条 → 放宽零新增命中） | #3：deep → emotion（34 字）✓ |
| 2. "像X，像Y，像Z"排比 | `_LIKE_PARALLEL_PATTERN`（≥3 项连续，项长 ≤12；"像你，像我"等 2 项日常表达不触发） | #6：deep → emotion（36 字）✓ |

**展示文本零影响**：detection_text 仅参与检测判定，render_for_user 输出路径不变；render_for_storage 零改动。

**测试**：91/91 全过（原 83 + 新增 8：2 条 context golden + 6 单测）｜checksum ALL PASS

**8-08 回放结果（渲染后）**：

| 指标 | 修复前 | 修复后 |
|------|:--:|:--:|
| mismatch rate | 2/12 (16.7%) | **0/12** ✓ |
| spec 分布 | daily 4 / deep 4 / emotion 3 / chat 1 | daily 4 / deep 2 / emotion 5 / chat 1 |
| 高情绪保护 | 2/2 ✓ | 2/2 ✓（不变） |
| 误伤 | 0 | 0（#2/#5 用户表白场景 deep 保留不变） |

**冻结纪律保持**：f6a2a14 未动（新 commit）；不 push、不打 tag，待统一发布确认。
