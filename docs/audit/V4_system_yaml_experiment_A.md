# V4 system.yaml 方案 A 实验报告（current_time 位置移动）

> 日期：2026-08-11（测试时段：凌晨 00:39~01:05）| 版本：v4-stage0.7 + loader 修复 + 真 Phase 1 + A
> 单变量：仅 current_time 注入位置（末尾 → 第 52 行身份区后），内容与文字完全不变
> 条件：同脚本 / deepseek-chat / temperature 0.7 / 13 场景 / phase0-baseline / 无历史

---

## 1. 修改内容（system.yaml 模板，移动 1 行）

- 前：`现在：{current_time}` 在模板最后（`{memory_context}` 之后）
- 后：`现在：{current_time}` 移到"你是绘梨衣。安静。真诚。温柔。"之后、"……是自然停顿。"之前

B 实验的 +7 行示例已回退。loader.py / eryi.yaml / 模型参数零修改。

## 2. dump 验证（✅ 全部通过）

- current_time 行号：108（末尾）→ **52**（前 1/2），**不在末尾** ✓
- 内容完全一致："现在：2026年8月11日 星期二 夏季 凌晨 00:39" ✓
- 完整 speaking_style ✓ / Phase 1 三规则 ✓ / 反文学化 ✓ / B 示例已回退 ✓

## 3. A 实验结果

### S3/S4/S7 情绪跟随（核心）

| 场景 | 输入 | A 实验输出 | 判定 |
|------|------|-----------|------|
| S3 | 今天好累 | 嗯……那个人是谁？ | ❌ **答非所问 + 幻觉**（无上下文编造"那个人"） |
| S4 | 我有点难过 | （轻轻点头）嗯……你回来啦。 | ❌ 未回应难过（"我来了"模板套用） |
| S7 | 我真的好害怕 | 嗯……睡不着吗。 | ❌ 仍深夜寒暄 |

**情绪跟随 0/3——未改善。**

### 四版真实验对比（13 次调用）

| 指标 | 真 baseline | 真 Phase 1 | +B | **A** |
|------|:--:|:--:|:--:|:--:|
| 深夜寒暄族 | 9/13 | 5/13 | 6~7/13 | 4~5/13（微降，含噪声） |
| 情绪跟随 S3/S4/S7 | 0/3 | 0/3 | ~0.3/3 | **0/3** |
| 固定模板逐字复读 | S6/S7 同句 ×2 | 0 | 0 | **S8-1/S8-2 复读 ×2** ⚠️ |
| 新异常 | S4 自述睡眠 | — | "……嗯"6/13 | **S3 幻觉** ⚠️ |
| 文学意象 | 0/13 | 0/13 | 0/13 | 0/13 ✅ |
| S8 轮4 跟随 | 错位 | 良好 | 最优 | 良好 ✅ |

### A 方案新异常明细

1. **S3 幻觉（最严重）**："今天好累"→"嗯……那个人是谁？"——单轮无历史场景下 LLM 凭空编造"那个人"，说明移走末尾时间后 LLM 失去锚定，开始无依据联想
2. **S8-1/S8-2 逐字复读**："嗯……还没睡呢。"连续两轮相同输出（输入分别为"你好"/"下班好累"）——固定复读回归
3. S4"你回来啦"——system.yaml"我来了→欢迎回来"示例的变体套用

## 4. 判断标准判定

| 标准 | A 结果 | 判定 |
|------|--------|------|
| S3/S4/S7 改善 | 0/3 | ❌ |
| 深夜寒暄下降 | 4~5/13（vs 5/13，噪声级） | ⚠️ 无明显下降 |
| 无新增模板 | S8 复读 + S3 幻觉 | ❌ |

**结论：current_time 位置不是单轮情绪跟随失败的主要因素。** 移动后情绪跟随仍 0/3、寒暄持平，且出现新的幻觉与复读异常——A 方案无效。

## 5. 下一步（用户既定流程）

按预定分支："不是位置问题 → 需要进一步测试 **current_time 内容弱化或场景化注入**"。

候选（下一轮单变量，等待决策）：
- A2：弱化文字（如去掉"深夜/凌晨"时段词，只留"2026年8月11日 星期二"）
- A3：场景化注入（仅用户主动问时间时才注入时间——需改 world_tracker 逻辑，属代码）

## 6. 数据与状态

- A 实验 JSON：`C:\Users\huali\AppData\Local\Temp\phaseA_output.json`
- 对比：`phase0_v2_output.json`（真 baseline）/ `phase1_v2_output.json`（真 Phase 1）/ `phaseB_output.json`（B）
- 未 commit 变更：`character/loader.py`（修复）、`character/characters/eryi.yaml`（Phase 1 +5）、`prompt/templates/system.yaml`（A：current_time 移至 52 行，B 已回退）
- 待用户统一决定 commit / 继续实验
