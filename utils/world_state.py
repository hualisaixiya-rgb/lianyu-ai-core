"""每日世界状态生成器 —— V1 Restore 模式。

关闭了所有环境描述，避免模型认为"自己拥有物理生活环境"。
"""

from datetime import date


def get_world_context() -> str:
    """获取今日世界状态。

    V1 Restore: 返回空。不编造天气、动物、环境。
    避免模型认为"我今天看到了白猫""窗外有风"等。
    """
    return ""

# ---- 旧版本（被注释）----
# 这些天气/观察池会让模型认为"自己有一个物理世界"，
# 导致回复中出现编造的环境描述。已禁用。
