# LianyuAI Core — 开发与部署工作流规范

## 角色划分

### 主机电脑（开发环境）
- **路径**：`E:\Visual Studio Code文件\lianyu-ai-core`
- **职责**：日常开发、功能修改、Prompt调整、本地测试、Git版本管理
- **原则**：主机是唯一开发入口，所有代码修改都在这里完成

### 服务器电脑（运行环境）
- **路径**：`E:\AI\lianyu-ai-core`
- **职责**：运行 LianyuAI 服务、运行 Telegram Bot、查看日志、服务重启
- **原则**：服务器不是开发环境。不直接改代码、不实验未确认版本

## 版本发布流程

```
开发阶段：  主机修改 → 本地测试 → 确认稳定

发布阶段：  主机 git add → git commit → git push
           → 服务器 git pull → 重启服务 → 查看日志 → 验证功能
```

**未经确认，不执行发布。**

## Git 规则

- Git 用于代码版本管理和已确认功能发布
- **禁止**：自动 push、自动部署、自动 pull 服务器
- 任何涉及服务器同步的操作，必须先询问

## 运行数据规则

以下属于运行数据，**不作为开发代码同步**：
- `archives/`
- `data/`
- `logs/`
- 数据库文件（`*.db`）
- 用户聊天记录
- 运行缓存

不要因为服务器产生的新日志反向覆盖主机项目。

## 配置文件规则

| 类型 | 示例 | 规则 |
|------|------|------|
| 代码配置 | `system.yaml`、Python代码、Prompt模板 | 属于开发内容，修改后走发布流程 |
| 运行配置 | `.env`、Token、API Key、服务器路径 | 主机和服务器可以不同，禁止覆盖服务器 `.env` |

## AI 助手行为要求

### 默认行为
- 所有修改操作默认针对**主机开发环境**
- 不自动同步、不自动 push、不自动部署

### 必须确认的操作
以下操作必须先询问用户：
- `git push`
- `git pull`
- 修改服务器文件
- 重启生产服务
- 删除数据
- 修改部署结构

### 判断标准
- 用户说"改代码/改Prompt/加功能" → 操作主机项目
- 用户说"发布/部署/重启服务器" → 执行发布流程
- 不确定时 → 先问

## 项目结构

```
lianyu-ai-core/
├── ai/              # AI Core 推理引擎
├── memory/          # Memory 系统
├── character/       # 角色定义
├── prompt/          # Prompt 模板
├── adapters/        # 平台适配器（Telegram等）
├── database/        # 数据库模型
├── config/          # 配置
├── scripts/         # 启动脚本
├── tests/           # 测试
├── docs/            # 文档
├── archives/        # 运行数据（不同步）
├── data/            # 运行数据（不同步）
├── logs/            # 运行数据（不同步）
└── backups/         # 备份（不同步）
```
